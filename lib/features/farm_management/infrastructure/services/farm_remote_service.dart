import 'package:famhub_app/features/farm_management/infrastructure/repositories/farm_repository_impl.dart';

/// Placeholder for remote API calls (Supabase / PostgREST).
///
/// This file exists so the infrastructure layer keeps remote concerns here,
/// and the repository contract stays stable for future Supabase wiring.
class FarmRemoteService {
  Future<void> noop() async {
    return;
  }
}

